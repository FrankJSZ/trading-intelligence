# Optional provider keys. The app works for market data and Yahoo news without keys.
FMP_API_KEY=
NEWS_API_KEY=

# Local SQLite journal. Relative paths are resolved from the project root.
TI_DB_PATH=data/trading_intelligence.db
